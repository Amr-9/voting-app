# Application
APP_PORT=8071
CORS_ALLOWED_ORIGINS=https://yourvotingsite.com

# MariaDB — credentials used by both docker-compose and the app
DB_ROOT_PASSWORD=R00t!Str0ng
DB_NAME=voting_db
DB_USER=voting_user
DB_PASS=Vt!9mKqR2@nXw7p
DB_HOST=mariadb   # Docker service name (use "localhost" for local dev outside Docker)

# Redis
REDIS_ADDR=redis:6379
REDIS_PASSWORD=

# JWT
JWT_SECRET=change_me_to_a_long_random_string_in_production

# Default admin credentials (used only on first run if admins table is empty)
ADMIN_DEFAULT_EMAIL=admin@yourvotingsite.com
ADMIN_DEFAULT_PASSWORD=Adm!nStr0ng@2025

# SMTP settings (Gmail example — use an App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your_gmail_app_password
SMTP_FROM=your@gmail.com

# Cloudflare Turnstile
TURNSTILE_SECRET=your_cloudflare_turnstile_secret_key

# Rate limiting: max OTP requests per IP per hour
RATE_LIMIT_MAX=5
